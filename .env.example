# Base URL of the target router. The Vite dev proxy forwards /goform and the
# router's static JS to this host so the app stays same-origin and localhost-only.
# Common ZTE factory addresses: http://192.168.0.1 or http://192.168.1.1
VITE_ROUTER_HOST=http://192.168.0.1
